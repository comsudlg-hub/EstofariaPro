import 'package:flutter/material.dart';

class clientdashboard extends StatelessWidget {
  const clientdashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' client dashboard.Value.ToUpper() lient  client dashboard.Value.ToUpper() ashboard'),
      ),
      body: const Center(
        child: Text(' client dashboard.Value.ToUpper() lient  client dashboard.Value.ToUpper() ashboard Screen'),
      ),
    );
  }
}
