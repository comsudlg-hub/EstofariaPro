import 'package:flutter/material.dart';

class workshopdashboard extends StatelessWidget {
  const workshopdashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' workshop dashboard.Value.ToUpper() orkshop  workshop dashboard.Value.ToUpper() ashboard'),
      ),
      body: const Center(
        child: Text(' workshop dashboard.Value.ToUpper() orkshop  workshop dashboard.Value.ToUpper() ashboard Screen'),
      ),
    );
  }
}
