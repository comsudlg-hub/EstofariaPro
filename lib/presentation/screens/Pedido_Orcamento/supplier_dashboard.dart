import 'package:flutter/material.dart';

class supplierdashboard extends StatelessWidget {
  const supplierdashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' supplier dashboard.Value.ToUpper() upplier  supplier dashboard.Value.ToUpper() ashboard'),
      ),
      body: const Center(
        child: Text(' supplier dashboard.Value.ToUpper() upplier  supplier dashboard.Value.ToUpper() ashboard Screen'),
      ),
    );
  }
}
