import 'package:flutter/material.dart';

class admindashboard extends StatelessWidget {
  const admindashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' admin dashboard.Value.ToUpper() dmin  admin dashboard.Value.ToUpper() ashboard'),
      ),
      body: const Center(
        child: Text(' admin dashboard.Value.ToUpper() dmin  admin dashboard.Value.ToUpper() ashboard Screen'),
      ),
    );
  }
}
