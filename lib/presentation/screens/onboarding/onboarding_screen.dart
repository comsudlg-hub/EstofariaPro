import 'package:flutter/material.dart';

class onboardingscreen extends StatelessWidget {
  const onboardingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' onboarding screen.Value.ToUpper() nboarding  onboarding screen.Value.ToUpper() creen'),
      ),
      body: const Center(
        child: Text(' onboarding screen.Value.ToUpper() nboarding  onboarding screen.Value.ToUpper() creen Screen'),
      ),
    );
  }
}
